[package]
name = "tech-log-parser"
version = "0.1.0"
edition = "2024"

[dependencies]
memchr = "2.7"
serde = { version = "1.0", features = ["derive"] }
chrono = { version = "0.4", features = ["serde"] }
smallvec = { version = "1.15", features = ["serde"] }
